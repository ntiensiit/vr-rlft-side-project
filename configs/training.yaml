supervised:
  batch_size: 2
  learning_rate: 0.001
  num_epochs: 3
  log_every: 50
rl:
  learning_rate: 0.0003
  num_updates: 10
  clip_ratio: 0.2
  entropy_coefficient: 0.0
  gamma: 0.99
  gae_lambda: 0.95
