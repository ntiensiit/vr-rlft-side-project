seed: 42
device: cpu
experiment_name: default
tracking:
  backend: none
  project: vr-rlft-side-project
  entity: null
  mode: offline
  mlflow:
    tracking_uri: ${oc.env:MLFLOW_TRACKING_URI,"./mlruns"}
    experiment_name: ${experiment_name}
paths:
  input_dir: ${oc.env:MGS_INPUT_DIR,"data"}
  output_dir: ${oc.env:MGS_OUTPUT_DIR,"artifacts"}
  ycb_mjcf: ${oc.env:MGS_INPUT_DIR,"data"}/processed/ycb_mjcf
  observations: ${oc.env:MGS_INPUT_DIR,"data"}/observations
  tensorboard: ${oc.env:MGS_OUTPUT_DIR,"artifacts"}/exports/tensorboard
