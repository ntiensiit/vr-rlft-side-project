# @package _global_
defaults:
  - /scripts/base
  - /scripts/grasp_model
  - _self_

script:
  checkpoint: ${model.checkpoint}
  output: ${model.exports.inference_candidates}
  method: ${default_method}
  feature_dim: ${architecture.feature_dim}
  num_steps: ${model.inference_steps}
  num_grasps: ${architecture.num_grasps}
  device: ${device}
  seed: ${seed}
  observation: null
  ycb_root: ${paths.ycb_root}
  object_id: null
