# @package _global_
defaults:
  - /scripts/base
  - /scripts/grasp_model
  - _self_

script:
  checkpoint: ${model.checkpoint}
  observations: ${observations.files}
  output: ${model.exports.grasp_candidates}
  feature_dim: ${architecture.feature_dim}
  num_grasps: ${architecture.num_grasps}
  device: ${device}
  seed: ${seed}
