# @package _global_
defaults:
  - default
  - _self_

notebook:
  experiment: evaluate_diffusion_flow_rl_colab

evaluation:
  method: diffusion
  analytical_report: ${paths.reports}/diffusion_analytical_evaluation_report.jsonl
  simulation_report: ${paths.reports}/diffusion_simulation_outcomes_${objects.ids.0}.jsonl
  filter_collisions: false
