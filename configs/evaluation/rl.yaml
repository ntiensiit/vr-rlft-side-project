# @package _global_
defaults:
  - default
  - _self_

evaluation:
  method: rl
  rollout_report: ${rl.exports.rollout_report}
  episodes: 5
  max_steps: 100
  stochastic: false
  exploration_noise: 0.1
